# Server Configuration
PORT=5000
NODE_ENV=development

# Cosmos DB Configuration
COSMOS_ENDPOINT=https://your-cosmos-account.documents.azure.com:443/
COSMOS_KEY=your-cosmos-primary-key
COSMOS_DATABASE_NAME=DigiTransacDB

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRE=7d

# CORS Configuration
CORS_ORIGIN=http://localhost:3000

# Frontend URL (for OAuth redirects)
FRONTEND_URL=http://localhost:5173

# Backend URL (for OAuth callbacks)
BACKEND_URL=http://localhost:5000

# Encryption Configuration
# For development: Use this key directly (same as stored in Azure Key Vault)
# For production: Leave empty - will fetch from Azure Key Vault automatically
MASTER_ENCRYPTION_KEY=gezRAFuYxa36CkI1bjSNBtdnmThrHD89
KEY_VAULT_URL=https://expense-tracker-kv-5963.vault.azure.net/

# Gmail OAuth Configuration
# Get these from: https://console.cloud.google.com/apis/credentials
# 1. Create a new project or select existing
# 2. Enable Gmail API
# 3. Create OAuth 2.0 Client ID (Web application type)
# 4. Add authorized redirect URI: http://localhost:5000/api/gmail/callback
# 5. Copy Client ID and Client Secret below
GMAIL_CLIENT_ID=your_gmail_client_id_here.apps.googleusercontent.com
GMAIL_CLIENT_SECRET=your_gmail_client_secret_here
